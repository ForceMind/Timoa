# 项目状态

更新日期：2026-09-16

## 当前阶段

阶段 3 主体已完成（模板库、周期规则、推荐、资金操作、中国特色需求第一批）→ 阶段 4（统计/预算/预测、家庭权限、导入导出、附件）。

## 已实现（阶段 3 新增）

- 完整种子库（spec 7.1–7.3）：13 支出组 + 7 收入组两级分类（带 emoji 图标）+ 100+ 模板 + 15 资金操作模板 + 6 预置标签；全部幂等（T36 测试：重跑不重复、不覆盖用户数据）。
- 模板管理：置顶（首页「常用」固定排序不被算法打乱）、停用；周期规则管理（我的 → 同页新增/停用）。
- 周期引擎：8 种频率；每月按日历、短月落月末且下月恢复锚定（T25 实测 1/31→2/28→3/31）；2/29 非闰年落 2/28；实例唯一键防重（重启/重复扫描不重复，T27/T29）；跳过/延后/停用（T28）。
- 周期确认：记账关联实例同事务累计；固定金额分次支付扣剩余计划；已处理实例拒绝重复关联（recurrence_already_processed）；启动 + 每日扫描，状态在数据库。
- 可解释推荐：到期规则 > 同星期习惯 > 最近使用 > 置顶/冷启动，最多 4 个带真实原因（「本期房租尚未确认」），推荐绝不入账。
- 资金操作（T14/T15）：借出/借入（应收应付，对方）、还本金（超额拒绝）、房贷车贷还款（本金减负债+利息计费用）、理财赎回（本金转回+已确认收益计收入）；押金经借出+结算语义。
- 支付优惠（中国特色）：discount 拆分——应付 10 实付 9，费用净额 9，优惠冲减费用。
- 重复记一笔：复制生成新业务 ID 与 operation_id（测试覆盖）。
- 关键词搜索：备注/商户/对方/分类。
- UI：首页待确认（去记/跳过）与动态推荐、记账预填、「保存并再记一笔」、更多类型（借出/借入/房贷还款/理财赎回）、优惠/代付录入、流水搜索、详情复制。
- 用户需求对照表 docs/FEATURE_MAP.md（24 条逐条评估与排期）。

## 已实现（阶段 4 第一批新增）

- 预算制度：总预算 + 分类预算（父分类覆盖子分类、父子不双算）、进度/剩余/超支、退款让剩余增加（测试覆盖）。
- 储蓄目标（存款制度）：目标金额/日期、绑定账户进度、完成标记。
- 统计区间：本月/上月/本季度/本年/自定义；环比（等长区间）与同比（去年同期），上期为零显示「—」不虚构百分比。
- ECharts 按需引入：收支趋势（柱+线）与分类构成（环形图），负净额分类不入饼图并明示。
- 资产负债：资产/负债/应收/应付/净资产，标注「按已录入账户和已记账记录计算」，溢缴保留负号。
- 推荐记忆：商户 → 分类学习（过账即学习、纠正即更新、不改历史）、上次金额提示；记账页输入商户自动建议分类（不覆盖手动选择）。
- 不再提醒：习惯/规则级屏蔽（✕），持久化到 recommendation_dismissals；周期实例单次「跳过」语义保留。
- 修复：单连接 SQLite 下游标未关闭导致的死锁（预算/储蓄目标/应收列表），并回归测试。

## 已实现（阶段 4 第二批新增）

- 账单导入：CSV/XLSX（UTF-8/GBK）、微信与支付宝预设（仅对合成样例测试格式声明兼容）+ 通用映射；预览不入正式账；状态过滤（已关闭/非收支不默认入账）；同来源单号强去重 + 无单号疑似重复提示；确认幂等（重复确认/重试不重复入账）；导入记录 date_precision=day 不伪造小时；批次撤销逐单留痕冲正、有退款关联的单据阻止并列出（T38/T39 测试）。
- 导出：CSV（BOM）/XLSX，含原金额/净额/渠道/商户/来源单号，口径与界面一致；公式注入防护（= + - @ 前缀）。
- 附件：图片/PDF 上传，魔数校验真实类型（伪装 EXE 拒绝）、5MB/5 个上限、随机存储名、授权接口访问（nosniff + CSP sandbox）、失败上传无垃圾引用（T41 部分）。
- 限制：文件 10MB/5000 行；XLSX 不执行公式。
- UI：我的 → 数据（来源选择 → 解析预览 → 分类/账户 → 确认/放弃、导出按钮、批次撤销）；账单详情附件上传/查看。

## 已实现（阶段 4 第三批新增）

- 家庭协作：邀请令牌（sha256 存储、单次使用、72 小时失效、可撤销，不要求邮件）；`/?join=` 加入页是唯一注册入口（公开注册关闭）；成员列表；撤销成员即归档 + 吊销全部会话 + 会话校验联动 archived 用户（旧会话立即 401）。
- 权限收紧（全部服务端校验）：成员不能建账户/分类/模板/规则/预算/导入（requireAdmin），普通成员只能更正自己创建的账单（CanEditTx），管理员全账更正。
- 现金流预测（spec 9.4）：预计月底净支出 = 已确认 + 剩余固定 + 可变估计（变动日均×剩余天，<7 天样本明示不足）；预计月底可用资金 + 缺口提示；固定计划与历史估计分开；已入账固定项不重复计入（测试覆盖）。
- 日历视图：月历格子显示每日收支与周期事项（含信用卡还款日，🗓️ 标记未入账）。
- 健壮性：Go 空切片统一返回 `[]` 而非 null（前端 .map 崩溃事故），前端数组字段兜底 `?? []`；main.tsx 全局错误兜底（渲染异常不再白屏）。

## 已实现（部署与备份恢复新增）

- 生产同源：前端构建产物嵌入 Go 单二进制（`scripts/build.sh`），SPA 回退、指纹资源 immutable 长缓存、index/API no-store。
- 一致性备份：VACUUM INTO 快照 + 附件 + manifest（sha256 清单），快照完整性/外键/借贷平衡校验，`.part` 临时文件完整后改名；age 口令加密可选（未配置明示 unencrypted）；每日自动（03:17 可配）+ 手动 + 保留 14 个；管理端立即备份/下载需重新输密码。
- 校验式恢复：临时目录校验（防穿越/版本/校验和/完整性/平衡）→ 当前数据安全备份 → 原子替换 → 撤销全部会话 + restore_generation；CLI `restore -yes` 需停服。
- 部署文件：多阶段 Dockerfile（非 root、healthcheck、单容器）、compose.yaml（127.0.0.1 绑定、named volume）、systemd unit（最小权限）、Nginx/Apache 片段、.env.example、DEPLOYMENT.md、BACKUP_RESTORE.md。
- 诊断：/api/v1/admin/diagnostics（版本/SQLite/迁移/备份状态）。

## 已验证（真实执行）

- 演练测试 TestBackupRestoreDrill（T42/T43）：恢复后概览/余额/退款链/附件一致；篡改包拒绝。
- 生产二进制实测：嵌入 SPA（HTML/缓存头/SPA 回退）、登录、管理端备份（re-auth）、诊断、CLI restore（校验→安全备份→恢复→旧会话 401）。
- `go test ./...` 全绿；前端 tsc/build 通过。
- 未验证（如实记录）：本机无 Docker，镜像构建未跑；生产 HTTPS 未验证。

## 已实现（阶段 5 新增）

- 生图图标集：Timoa logo（时钟+嫩芽，192/512/maskable）+ 5 个导航图标（首页/流水/＋/统计/我的），替换全部文字符号图标；底部 + 号改为高对比绿色圆形生图按钮；水印清理与多尺寸处理。
- PWA：manifest（standalone/图标/主题色）、Apple Touch Icon、Service Worker（只缓存公开应用壳，不缓存 API/凭据/财务数据，导航离线回退、指纹资源缓存优先）。
- 离线队列：IndexedDB outbox（本机保存成功才反馈，失败保留输入并警示）；可信设备开关（共享设备默认不存财务副本）；断网记账 →「已保存到本机（待同步）」；恢复网络/回前台/手动触发同步；未同步内容可本机导出补救。
- 同步协议：/sync/push（批量逐操作结果、服务端幂等最终保障、世代校验）与 /sync/pull（rowid 单调游标 + 恢复世代 + 账户分类快照）；世代不一致 409 并提供全量重同步；会话过期暂停同步。

## 已实现（阶段 6 收尾新增）

- T45 外观：深色主题（跟随系统/浅色/深色三档）+ 大字模式。CSS 变量双套（`<html data-theme>` 切换），分类底色深浅两套按主题取值，ECharts 图表随主题重建配色（canvas 不随 CSS 变量自动更新）；index.html 内联脚本防首屏闪烁；偏好存本机 localStorage（纯展示设置，不入库不同步）。
- 入口：我的 → 设置 → 外观（主题三档分段、字号两档分段），即时生效。

## 已验证（真实执行）

- curl：push 幂等（重放 replayed 不重复）、pull 游标、世代不一致 409 / 当前世代 200。
- 浏览器实测：图标渲染、SW active、断网（杀后端）入队 → flush 失败保留 → 恢复后 flush 成功且服务器恰一笔。
- 修复：decodeJSON 严格字段校验误杀离线队列元数据字段（400 invalid_input）。

## 未完成 / 下一步

- 阶段 6 收尾：Docker 镜像构建验证（需有 Docker 的机器）、T22 base_version 冲突细化、端到端回归。
- 便笺 UI、子账户、储值券效期、分摊计划、多人开销配比、农历/法定工作日日历（docs/FEATURE_MAP）。
- PWA 增强：安装引导 UI、iOS 真机验证（浏览器模拟不能冒充）。

## 已知限制

- 还本金/收本金目前经 API 完成，UI 入口在待收往来（阶段 4 补齐）。
- 未做生产 HTTPS 验证；未做真机验证。
- 性能测试已执行：10 万笔流水实测通过（详见 docs/ACCEPTANCE.md「性能测试」），瓶颈为资产负债/账户余额的全量分录扫描，亚秒级可接受。

## 已实现（阶段 2 新增）

- 拆分：记账支持「自担费用 + 代付应收」拆分（counterparty），拆分表 + 按拆分项退款分摊。
- 真实退款：全额/部分/多次/按拆分项/退至不同账户；写事务内额度重校验（T02 并发测试）；代付部分退款冲减应收不重复结算（T09）。
- 报销/代付：结算回款（部分/多次/账龄/超额拒绝）、普通支出转待报销（重分类，无现金流）、核销（原因必填）。
- 收入退回：关联原收入，累计 ≤ 原额（T10）。
- 不可变修订链：更正（冲正 + 替代版本，业务日期保持）与作废；revisions 原单唯一约束防重复冲正；有下游关联阻止作废（dependency_blocked）。
- 有效业务投影：余额读全部分录，统计只读有效投影；跨月双口径（发生期/原消费归属，后者截至统计日）CategoryNets。
- 统计 API：overview（7 项）、categories（双口径）、receivables（待收/账龄）。
- UI：账单详情二级页（退款/收入退回/回款/核销/转待报销/更正/作废行内操作）、记账「更多」代付录入、统计页概览/分类双口径切换/待收往来。
- 品牌视觉：按设计稿落地 Timoa·小账（前次提交）。

## 已验证（真实执行）

- `go test ./...` 全绿：T01–T13 + 核销 + 并发退款 + 阶段 1 回归；`go vet`、前端 tsc/build 通过。
- curl E2E：拆分单 → 回款 → 超额拒绝（settlement_cap_exceeded）→ 详情 → 概览口径全部正确。
- 浏览器走查：详情页退款 200 → 退款记录/概览（费用 600、退款 200、净支出 400）/余额 2600.00 全部联动正确。
- 验收记录已建 docs/ACCEPTANCE.md（T01–T50 逐项状态）。

## 未完成 / 下一步

- 阶段 3：完整中文分类/模板种子（spec 7.1–7.3）、模板管理、周期规则与实例、可解释推荐、两级 IA 收尾。
- 资金操作语义（借贷/押金/房贷本息 T14–T15）随阶段 3 模板实现。

## 已知限制

- 重分类仅支持无拆分支出单（拆分单在创建时直接拆应收）；更正暂按整单金额，拆分单更正在后续阶段细化。
- 未做生产 HTTPS 验证；未做真机验证；性能测试未执行。

## 已实现（阶段 1 新增）

- 认证：Argon2id（OWASP 参数 m=19MiB/t=2/p=1）、服务端会话（HttpOnly+SameSite=Lax cookie、30 天、可吊销）、登录限速（5 次/15 分钟）、关闭公开注册、`init-admin` 本地初始化（已有管理员拒绝执行）、`reset-password` 本地恢复（吊销会话+审计）。
- 账户：9 类资金账户 + 科目映射、期初余额可暂不确认（`balance_unconfirmed`）、归档（有交易不可删）、余额=期初+完整分录求和（负债含溢缴负值）。
- 账务核心：复式分录（借贷平衡防御校验）、整数分、`POST /transactions` 幂等（同内容重放/异内容 409）、交易+分录+审计单事务提交、跨账本引用拒绝、归档账户拒绝入账。
- 前端：登录、首账户引导、快速记账（支出/收入/转账 + 分类 + 账户 + 备注）、月汇总卡片、账户余额列表、最近流水；真实空状态无假数据。
- CSRF：SameSite=Lax + Origin 校验；开发代理来源用 `XIAOZHANG_ALLOWED_ORIGINS` 显式配置。
- 安全头：nosniff / DENY / no-referrer；API no-store。

## 已验证（真实执行）

- `go build ./... && go vet ./...` 通过；`go test ./...` 全绿（money 解析/溢出、借贷平衡不变量、转账不计收支、信用卡负债/还款/溢缴、幂等重放与冲突、非法输入与跨账本拒绝、归档拒绝、未确认余额标志、Argon2id、限速器）。
- curl 端到端：init-admin → 登录 → 建账户（期初 1000.00）→ 支出 28.00 → 幂等重放返回同 tx → 异内容 409 → 汇总 expense 2800 → 余额 97200 ✓。
- 内置浏览器实机走查：登录 → 创建账户 → 记 28 元餐饮 → 卡片/余额/流水全部正确刷新。
- 修复并验证：vite 代理下 Origin/Host 不一致导致的误拦截（引入显式允许来源配置）。

## 未完成 / 下一步

- 阶段 2：退款、报销/代付回款、错账更正与作废、收入退回、跨月统计口径、不可变冲正链。
- 静态产物嵌入 Go 同源托管、完整中文分类/模板种子、两级 IA、PWA、部署与备份（按阶段推进）。

## 已知限制

- 未做生产 HTTPS 验证；未做真机验证；性能测试未执行。
- 阶段 1 暂以「转账」覆盖信用卡还款语义（资金操作模板在阶段 3 细化）。
- 阶段 1 会话为单账本单角色取值（多账本/成员管理在阶段 4）。
