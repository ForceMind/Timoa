# Nginx 反向代理片段（同源：静态 + API 一起转发）
# 放进既有站点的 server {} 内；不独占 80/443 之外的端口。

location /xiaozhang/ {
    # 若整站子路径部署；根路径部署则直接用 location /
    proxy_pass http://127.0.0.1:8787/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    # 应用只信任直接对端；不要转发任意外部 X-Forwarded-For
    proxy_set_header X-Forwarded-For "";
    proxy_read_timeout 30s;
    client_max_body_size 12m;   # 覆盖导入文件与附件上限
}
